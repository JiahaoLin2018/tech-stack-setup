# 部署环境（由 actions/start.md 自动注入，无需手动修改）
# 合法值：dev | sit | fat | uat | prod
CONSUL_ENV=dev

# Consul 端口配置
CONSUL_HTTP_PORT=8500
CONSUL_DNS_PORT=8600

# 日志等级（dev 排障可改 INFO 或 DEBUG）
CONSUL_LOG_LEVEL=WARN

# 注意：Gossip 加密 / ACL 配置不在 .env 中维护
# 如需启用，编辑 conf/consul.hcl.tpl 对应注释段（含完整启用步骤和密钥生成命令）
