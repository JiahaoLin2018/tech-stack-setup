# IP 白名单配置
# 用于 whitelist 模式的域名
#
# 说明：
#   - 此文件为模板，由 add-route action 动态生成
#   - 每个白名单域名会生成独立的 allow/deny 规则
#   - 直接 include 到 server block 中使用
#
# 示例（由 add-route 生成）：
#   allow 192.168.1.0/24;
#   allow 10.0.0.0/8;
#   deny all;

# 默认配置（空）
# 白名单规则由 add-route 动态添加
