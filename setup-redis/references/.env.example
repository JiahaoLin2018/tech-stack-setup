# 密码生成规则：{服务缩写}{角色}_{16位随机大小写字母+数字}，总长 24-28 位
# 本文件中需替换的密码字段及对应标签：
#   REDIS_PASSWORD → RdsDefault_{16位随机}（管理员）
#   REDIS_APP_PASSWORD → RdsApp_{16位随机}（业务应用）
#   REDIS_EXPORTER_PASSWORD → RdsExp_{16位随机}（监控采集）
# 示例：RdsApp_aB3kP7mN9xQ2wE5r
# 部署完成后密码记录在 env/redis.md

# Redis 生产环境配置
# 复制为 .env 并修改所有 CHANGE_ME_* 值

# 部署环境（dev|sit|fat|uat|prod）
ENV=dev

# === Redis Admin (default user, full permissions, management only) ===
REDIS_PASSWORD=CHANGE_ME_REDIS_DEFAULT_PASSWORD

# === App User (business applications, +@all minus destructive commands) ===
REDIS_APP_USER=app
REDIS_APP_PASSWORD=CHANGE_ME_REDIS_APP_PASSWORD

# === Server ===
REDIS_PORT=6379
# Redis 应用层内存限制（maxmemory）
# dev/sit=512mb, fat/uat=1g, prod=2g
REDIS_MAX_MEMORY=512mb

# === 容器资源限制 ===
# 容器内存需预留空间给 Redis fork（RDB/AOF 重写）、碎片整理
# 建议：容器内存 = maxmemory × 1.5~2
# dev/sit=1g(512mb×2), fat/uat=2g, prod=4g
REDIS_MEMORY_LIMIT=1g
REDIS_MEMORY_RESERVATION=256m

# === Prometheus Exporter ===
REDIS_EXPORTER_PORT=9121
REDIS_EXPORTER_USER=exporter
REDIS_EXPORTER_PASSWORD=CHANGE_ME_EXPORTER_PASSWORD
