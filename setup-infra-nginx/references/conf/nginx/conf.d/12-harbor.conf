# Harbor Web + Registry
# 特殊配置：无大小限制 + 关闭请求缓冲
server {
    listen 80;
    server_name harbor.renew.com;

    client_max_body_size 0;
    proxy_request_buffering off;

    location / {
        proxy_pass http://${HARBOR_HOST}:${HARBOR_PORT};
        include /etc/nginx/proxy_params;
    }
}
