# 默认 server - 健康检查 + catch-all
server {
    listen 80 default_server;
    server_name _;

    # 健康检查端点
    location /health {
        return 200 '{"status":"UP"}';
        add_header Content-Type application/json;
    }

    # 未匹配的域名返回 444（关闭连接）
    location / {
        return 444;
    }
}
